# 《伟大航路·本部决战》V53 源码迁移说明

## V53 主角与敌人技能姿势、冲击特效优化（2026-09-23）

- Film Red 主角的运行时骨骼动作按普通拳、连打、飞拳、战斧、火箭、爆发、霸气和巨人拳分层处理，加入蓄力、释放、重心偏移、躯干扭转和收招姿势。
- 普通命中、重击和技能命中增加三维能量核心、环形冲击波、低面数碎片、击退、命中闪光和屏幕震动；不依赖单张二维技能贴图。
- 飞拳与枪弹改为低面数三维能量体，包含核心、环带与锥形拖尾；命中后自动生成独立爆发效果。
- 赤犬圆形与直线危险区域改为三维几何预警，并加入蓄力核心、Boss技能姿势、释放爆发与阶段反馈。
- 新增世界特效数量上限和统一清理逻辑，避免连续战斗后几何体、材质和粒子持续积累，优先保证安卓手机稳定性。

## V52 敌兵 3D 动作优化（2026-09-23）

- 移除海军小兵旧的 2D 行走、攻击、射击图集，不再把平面贴图当作敌兵。
- 普通刀兵、枪兵、盾兵、队长统一映射到模型库中的玩偶突击/远程/重装和加普 FBX。
- 每个敌兵使用真实 FBX 的 SkinnedMesh 骨骼节点，运行时加入待机呼吸、走路摆臂摆腿、攻击前摇/命中/收招、受击倾斜与闪烁。
- 敌兵转向改为平滑插值，移动和攻击动作与实际命中窗口同步。
- FBX 资源按 Z 轴转为 Three.js Y 轴并重新计算脚底高度，修复横躺、悬空和飞行姿态。
- 模型加载完成前隐藏整个敌兵根节点，失败时明确提示，不显示漂浮血条或 2D 占位。

## V48 第一阶段修复（2026-09-23）

- 接入模型库中的加普、伪草帽三人、三种玩偶兵和赤犬 FBX。
- 新增异步加载保护：真实模型成功前隐藏敌兵根节点；失败时给出明确提示，不再出现只有血条或平面占位。
- Film Red 加载失败信息改为明确区分“真实模型”和“2D备用角色”。
- 赤犬世界空间血条关闭，Boss HUD 固定到屏幕顶部并增加血量百分比、阶段文字。
- 当前锁定继续显示脚下锁定圈、目标名称/血量/距离，并增加金色锁定准星。
- 资源审计见 `MODEL-RESOURCE-INVENTORY.md`。

本压缩包由用户上传的 V47 最新源码继续修改，不是 GitHub `main` 中的旧版 V28。

## 当前版本重点

- 页面版本：V53
- 默认主角：Film Red 路飞
- 默认模型路径：`assets/models/film-red-luffy/luffy022_body_model.fbx`
- Film Red 配套贴图：同目录下 `luffy022_*.png`
- 模型实测：32,549 三角面、6 个蒙皮网格、247 个骨骼节点
- 当前 FBX 没有内置动作剪辑；走路、攻击、呼吸等动作由 `game.js` 运行时驱动骨骼
- 新兵种：加普精英队长、伪娜美、伪狙击手、三种玩偶兵；普通刀/枪/盾/队长槽位全部使用上述真实3D模型
- Boss：赤犬；世界血条已关闭，Boss 血条固定在屏幕顶部
- 锁定系统：显示当前目标名称、血量、距离，并在目标脚下显示金色锁定圈
- 安卓横屏优先；左摇杆移动，右侧区域滑动控制视角

## 运行方法

请通过本地 HTTP 服务或静态网站托管运行，不要直接双击 `index.html`。

例如进入本目录后运行：

```bash
python -m http.server 8080
```

浏览器打开 `http://localhost:8080/`。

## 迁移注意

- 保持现有目录结构，尤其不要移动 `assets/models/film-red-luffy/`。
- `index.html` 当前加载 `style.css?v=53` 与 `game.js?v=53`。
- Three.js、GLTFLoader 与 FBXLoader 当前通过 jsDelivr CDN 加载，运行时需要网络。
- 第一人称会隐藏第三人称世界角色，避免出现重复身体。
- 后续以本 ZIP 为新项目源码基线；旧聊天和 GitHub `main` 只作参考。
