# 《伟大航路·本部决战》V48 源码迁移说明

## V48 第一阶段修复（2026-09-23）

- 接入模型库中的加普、伪草帽三人、三种玩偶兵和赤犬 FBX。
- 新增异步加载保护：真实模型成功前始终显示备用3D兵模；失败时给出明确提示，不再出现只有血条的空角色。
- Film Red 加载失败信息改为明确区分“真实模型”和“2D备用角色”。
- 赤犬世界空间血条关闭，Boss HUD 固定到屏幕顶部并增加血量百分比、阶段文字。
- 当前锁定继续显示脚下锁定圈、目标名称/血量/距离，并增加金色锁定准星。
- 资源审计见 `MODEL-RESOURCE-INVENTORY.md`。

本压缩包由用户上传的 V47 最新源码继续修改，不是 GitHub `main` 中的旧版 V28。

## 当前版本重点

- 页面版本：V48
- 默认主角：Film Red 路飞
- 默认模型路径：`assets/models/film-red-luffy/luffy022_body_model.fbx`
- Film Red 配套贴图：同目录下 `luffy022_*.png`
- 模型实测：32,549 三角面、6 个蒙皮网格、247 个骨骼节点
- 当前 FBX 没有内置动作剪辑；走路、攻击、呼吸等动作由 `game.js` 运行时驱动骨骼
- 新兵种：加普精英队长、伪草帽三兵种、三种玩偶兵，以及原海军兵种
- Boss：赤犬；世界血条已关闭，Boss 血条固定在屏幕顶部
- 锁定系统：显示当前目标名称、血量、距离，并在目标脚下显示金色锁定圈
- 安卓横屏优先；左摇杆移动，右侧区域滑动控制视角

## 运行方法

请通过本地 HTTP 服务或静态网站托管运行，不要直接双击 `index.html`。

例如进入本目录后运行：

```bash
python -m http.server 8080
```

浏览器打开 `http://localhost:8080/`。

## 迁移注意

- 保持现有目录结构，尤其不要移动 `assets/models/film-red-luffy/`。
- `index.html` 当前加载 `style.css?v=47` 与 `game.js?v=47`。
- Three.js、GLTFLoader 与 FBXLoader 当前通过 jsDelivr CDN 加载，运行时需要网络。
- 第一人称会隐藏第三人称世界角色，避免出现重复身体。
- 后续以本 ZIP 为新项目源码基线；旧聊天和 GitHub `main` 只作参考。
