// Texture loading is intentionally kept out of the generator. The game assigns
// compact, local PBR maps after geometry generation so all tree instances can
// share them without embedding every EZ-Tree species in the JavaScript bundle.
export function getBarkTexture(){ return null; }
export function getLeafTexture(){ return null; }
